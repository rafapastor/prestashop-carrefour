/*
 * SPDX-License-Identifier: AGPL-3.0-only
 *
 * Admin JS for the Carrefour Marketplace Connector.
 * Currently a placeholder — most interactivity is handled server-side via
 * plain form submissions. This file exists so the hookDisplayBackOfficeHeader
 * loader has a file to enqueue and doesn't 404.
 */
(function () {
    'use strict';

    /* Placeholder namespace in case future enhancements need JS. */
    if (typeof window.Carrefour === 'undefined') {
        window.Carrefour = {};
    }
})();
